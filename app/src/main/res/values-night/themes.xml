<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base application theme. -->
    <style name="Theme.UNORPproject" parent="Theme.AppCompat.Light.NoActionBar">

        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/statusbarr</item>
        <item name="colorPrimaryDark">@color/statusbarr</item>
        <item name="colorPrimaryVariant">@color/statusbarr</item>
        <item name="colorOnPrimary">@color/white</item>

        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">@color/statusbarr</item>

        <!-- Customize your theme here. -->
        <item name="colorSurface">@android:color/transparent</item>

    </style>

</resources>