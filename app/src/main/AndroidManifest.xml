<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.unorpproject">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.UNORPproject"
        android:networkSecurityConfig="@xml/network_security_config"
        >

        <activity
            android:name=".AreaAddItem"
            android:exported="true"
            android:parentActivityName=".AreaLogged" />
        <activity
            android:name=".AreaLogged"
            android:exported="true"
            android:parentActivityName=".LoginForm" />
        <activity
            android:name=".AreaUsuario"
            android:exported="true"
            android:parentActivityName=".AreaLogged" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:parentActivityName=".LoginForm" />
        <activity
            android:name=".CadastroForm"
            android:exported="true"
            android:parentActivityName=".LoginForm" />
        <activity
            android:name=".LoginForm"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>